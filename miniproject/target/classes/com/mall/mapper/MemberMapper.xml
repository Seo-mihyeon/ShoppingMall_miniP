<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mall.mapper.MemberMapper">

<!-- adminCk는 일반회원 등록을 디폴트값으로 지정하기 위해 0으로 삽입함 -->
	<insert id="memberJoin">
		<![CDATA[
			INSERT INTO SHOP_MEMBER VALUES(#{memberId}, #{memberPw}, #{memberName}, #{memberPhone},
								#{memberBirth}, #{memberMail}, #{memberAddr1}, #{memberAddr2}, #{memberAddr3}, 0)
		]]>
	</insert>
	
	<!-- id 중복 검사  -->
	<select id="idCheck" resultType="int">
		<![CDATA[
		SELECT COUNT(*) FROM SHOP_MEMBER WHERE MEMBERID = #{memberId}
		]]>
	</select>
<<<<<<< HEAD

	<select id="memberCheck" resultType="int">
		<![CDATA[
		SELECT * FROM SHOP_MEMBER WHERE MEMBERID = #{memberId}
=======
	
	<!-- 로그인 / memberId, memberName, adminCk 열을 조회할 것 -->
	<select id="memberLogin" resultType="com.mall.model.MemberVO">
	<!-- 기존 코드 
		SELECT MEMBERID, MEMBERNAME, ADMINCK FROM SHOP_MEMBER 
			WHERE MEMBERID = #{memberId} AND MEMBERPW = #{memberPw}
	 -->
		<![CDATA[
		SELECT MEMBERID, MEMBERPW, MEMBERNAME, ADMINCK FROM SHOP_MEMBER WHERE MEMBERID = #{memberId}
>>>>>>> ed7d5f88136ba1ebe6fa5f49cc31ed7c3a5b548a
		]]>
	</select>
</mapper>
